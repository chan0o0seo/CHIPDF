# 첫 구현 보고서 — 2026-09-11

새 폴더에서 **카드 한 장을 직접 편집하는 네이티브 시제품 0.1**을 구현했다. 기획서 단계 B의 편집·저장·PNG 출력 경로를 확인했다. 번역 엔진 선정과 세로쓰기 검증은 남아 있으므로 단계 B 전체 또는 V1 완료로 표시하지 않는다.

## 이번 구현

- PySide6 자체 창, 왼쪽 카드 썸네일, 중앙 편집 화면, 선택 시에만 나타나는 서식 도구.
- PNG/JPG/BMP 열기, 편집 화면에 파일 끌어 놓기.
- 텍스트 추가, 한글 입력, 더블클릭/Enter로 편집, Esc로 상자 선택 상태로 전환.
- 문장 일부의 글꼴·크기·색·굵게·기울임·밑줄, 문단 정렬.
- 상자 이동·크기·회전, 다중 선택, 복제·삭제, 실행 취소·다시 실행.
- 흰 상자 배경, 원본 비교, 글자 넘침 표시, 확대·화면 맞춤.
- 원본 사본과 구조화된 문단·서식을 담는 `.twproj`, 자동 저장, 직전 저장본 백업, 재열기.
- 편집 장면과 같은 Qt 텍스트 렌더링 경로를 사용하는 원래 크기 PNG 출력.

프로젝트는 `manifest.json`과 `assets/original.png`를 담은 ZIP 컨테이너다. 디렉터리형 실험 대신 한 파일로 이동 가능한 `.twproj`를 첫 형식으로 선택했다. 임시 파일을 완성한 후 원자적으로 교체한다. 원문 영역·제거 영역·출력 상자 좌표는 별도 필드다. 기존 앱 파일이나 사용자 원본을 수정하지 않았다.

## 검증

Qt 위젯에 입력·마우스 이벤트를 전달하는 자동 검사 **12개 통과**:

- 한글 preedit는 저장된 본문에 들어가지 않으며 commit 이후에만 반영됨.
- 입력 중 방향키는 커서, 선택 상태 방향키는 상자를 이동함.
- 더블클릭 편집 진입, Shift 다중 선택, 실제 마우스 이벤트의 이동·크기·회전.
- 부분 서식과 한글 전체 교체 후 기본 서식 보존.
- 텍스트/상자 실행 취소·다시 실행, 복제·삭제·복원.
- 저장 실패 시 이전 파일 보존, 손상 파일이 현재 작업을 대체하지 않음.
- 자동 저장·복구·직전 백업, 원문/제거 영역 독립성.
- 재열기 전후 출력 픽셀 일치, 선택 테두리·원본 비교 상태가 출력에 섞이지 않음.

사용자가 제공한 `フラジャイル・ウェイト`의 실제 카드 `キャラ-エンジニア_a-1「悪夢」.png`에서도 720×540 원본을 열고 시험용 한글 상자를 배치했다. 저장 후 다시 연 PNG가 바이트 단위로 동일했고 원본 SHA-256도 유지됐다. 시험 문장은 실제 번역 결과가 아니다. 산출물은 `qa/real-card`에만 저장했다.

이 과정에서 한글 전체 교체 후 Qt 기본 글자색을 잘못 저장하는 문제를 발견해 수정했다. 위 검사는 Qt 입력 이벤트를 사용하는 시험이다. 실제 Windows 한국어 IME 후보 창, 다른 디스플레이 배율, 다른 PC에서의 사용 검증은 별도로 남아 있다.

## 배포와 남은 작업

개발 도구를 별도로 설치하지 않는 이동식 Windows 실행 파일을 빌드했다. 최종 빌드는 `20260911-180242-48e68b`이며 `Start Translation Studio.cmd`가 이 빌드를 실행한다.

ZIP을 새 위치에 풀고 개발용 Python/Qt 환경 변수와 개발 도구 PATH를 제외한 상태에서 실제 창·프로젝트 열기·PNG 출력을 확인했다. 종료 코드 0, 개발판과 PNG 바이트 일치. 실행 파일 SHA-256은 `e904594fb2d274dc1d3e6f54adeca6f361ac91adaebc7738abb5422794527612`다. 검증 기록은 `qa/portable-result.json`에 있다.

처음에는 개발 환경의 Poppler용 `icuuc.dll`이 Qt 배포본에 섞여 시작 시 DLL 오류가 났다. 빌드 프로세스의 DLL 탐색 경로를 Qt와 Windows 시스템으로 한정해 해결했다. 기존 프로그램이나 시스템 PATH는 변경하지 않았다.

설치 마법사, 업데이트, 깨끗한 Windows 가상 환경 검증은 아직 단계 F의 과제다. 실행 위치와 작업 데이터 위치를 분리했다.

자동 번역·OCR·원문 자동 제거·PDF, 세로쓰기, 도형·삽입 이미지·그룹·정렬 도구와 기존 작업 가져오기는 아직 미구현이다. 자동 번역 버튼을 완성한 것처럼 노출하지 않았다.

로컬 엔진 후보는 20개 합성 문장에서 실제 실행했으나, 의미 누락·고유명사 변경·영어 혼입을 확인해 기본 엔진으로 채택하지 않았다. [검토 결과](ENGINE_REVIEW.ko.md)를 기준으로 다음 후보를 검증한 뒤 카드의 원문 인식 → 번역 → 배치 흐름을 연결한다.

다음 개발 순서:

1. 직접 일본어→한국어 모델 후보와 Windows 한국어 IME·세로쓰기 검증을 이어서 단계 B를 마무리한다.
2. 기존 OCR·글자 제거 처리의 필요한 부분을 새 앱 내부 서비스로 이식한다.
3. 단계 C에서 실제 카드 한 장의 열기 → 번역 → 직접 수정 → PNG 출력을 연결한다.
