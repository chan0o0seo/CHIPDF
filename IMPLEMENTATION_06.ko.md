# 여섯 번째 구현 — 세로쓰기·서식·배치 재사용 · 2026-09-13

0.6은 카드 위에서 세로 한글을 직접 고치고 같은 형식의 카드를 반복 편집하는 기능을 추가한다. 기존 화면을 유지하며 `서식` 메뉴에 세로쓰기와 이름 있는 서식을, `배치` 메뉴에 글상자 배치 저장·적용을 넣었다.

## 동작

| 작업 | 구현 내용 |
|---|---|
| 쓰기 방향 | 선택한 텍스트 상자를 가로/세로로 변경. 위에서 아래, 오른쪽 열에서 왼쪽 열로 배치. 문구·부분 서식·좌표·원문 연결 유지, 한 번에 실행 취소 |
| 세로 편집 | 같은 상자에서 한글 조합 입력, 마우스·키보드 선택, 부분 서식, 방향키 이동, Enter·Shift+Enter, 글자 삭제·실행 취소 |
| 문자 경계 | Qt의 UTF-16 위치와 그래핌 경계를 연결. 이모지 묶음·결합 문자를 방향키·삭제 중 잘라 내지 않음 |
| 표시와 출력 | 문단 정렬·열 간격·문단 여백, 넘침 표시, 그룹 회전·배율을 공통 렌더러로 표현. PNG·PDF·썸네일 같은 배치 |
| 글자 서식 | 첫 글자와 첫 문단의 설정, 안쪽 여백·쓰기 방향에 이름을 붙여 개인 설정에 저장. 선택한 텍스트 상자 전체에 적용 |
| 배치 재사용 | 같은 카드 크기·같은 글상자 수에서 문구별 대응과 이동 위치를 확인한 뒤 선택 카드에 적용. 기본은 배치만, 필요하면 저장한 서식도 적용 |
| 내용 보존 | 서식과 배치를 적용해도 카드마다 다른 문구·원문·원문 제거 좌표·검토 상태 유지. 여러 카드 변경도 한 번에 실행 취소 |
| 저장 | 버전 6 프로젝트에 쓰기 방향 보관. 이전 버전 1~5 읽기, 기존 가로 출력 유지. 개인 서식은 별도 `presets.json`에 영구 저장 |

세로 표시·클릭·커서 이동은 `vertical_text.py`의 공통 배치를 사용한다. 실제 입력·클립보드·실행 취소·IME 교체는 Qt 텍스트 문서가 처리한다. 조합 중인 문자는 복제 문서에서만 그려 자동 저장에 포함하지 않는다. 세로 텍스트 영역의 클릭 판정은 가로 Qt 문서 높이에 묶이지 않도록 처리했다. 선택한 굵은 문구 전체를 한글 입력으로 바꿔도 해당 입력 서식을 유지한다.

이름 있는 서식은 상자의 첫 글자와 첫 문단을 대표로 삼으며 적용 시 모든 문단·글자 서식을 통일한다. 배경 채우기·불투명도와 번역 검토 기록은 바꾸지 않는다. 배치는 보이는 영역 기준 위→아래, 같은 높이면 왼쪽→오른쪽 순서로 대응한다. 적용 창에서 카드별 문구와 이동 위치를 보여 준다. 그룹 안 텍스트나 잠긴 텍스트가 있는 카드, 픽셀 크기 또는 상자 수가 다른 카드는 적용 대상에서 제외한다. 전체 변경을 검증한 뒤 반영하므로 일부 카드만 바뀌고 실패하지 않는다.

개인 서식·배치에는 원문이나 번역문을 저장하지 않는다. `%LOCALAPPDATA%/TranslationStudio/presets.json`을 원자적으로 저장하며 손상된 파일·지원하지 않는 버전·다른 앱 창의 변경을 발견하면 기존 파일을 덮어쓰지 않는다. 목록은 해당 PC의 개인 설정이고 프로젝트에는 적용 결과가 저장된다.

## 검증

**100개 행동 테스트를 통과했다.** 결과는 `qa/tests-06.log`에 기록한다. 기존 72개 텍스트·OCR·번역·이미지·그룹·PDF·여러 카드 시험에 세로 문자 경계·열 배치·IME 교체·부분 선택·실행 취소·옛 클립보드 호환과 개인 서식 재실행·파일 오류·여러 카드 적용 시험 28개를 추가했다.

실제 카드 원본 3장을 별도 QA 프로젝트로 복사해 검증용 문구와 바탕을 추가했다. 네이티브 Windows 창에서 한글 조합·입력, 세로 부분 크기·굵기 변경, 서식 저장 후 편집기 재실행, 두 카드 배치 적용과 되돌리기, 회전·축소 그룹을 확인했다. 세 페이지 모두 원본 렌더링·PNG 묶음 출력·프로젝트 재열기의 PNG가 일치했다. 출력 PDF 세 페이지의 물리적 크기를 확인하고 Poppler로 렌더링해 검토했다. 이전 QA 프로젝트와 포함된 원본 이미지의 해시도 유지됐다. 이 문구는 기능 시험용 예시이며 실제 작품의 번역 완료본이 아니다.

- `qa/vertical-06/result.json`: 네이티브 시험 결과·PNG 해시·PDF 크기
- `qa/vertical-06/작업.twproj`: 별도 검증 프로젝트
- `qa/vertical-06/vertical-selected-window.png`, `ime-preedit-window.png`, `layout-preset-dialog.png`: 실제 편집 창
- `qa/vertical-06/pdf-render-1.png`~`pdf-render-3.png`: 출력 PDF 검토 이미지
- `verify_portable.py`: ZIP 재배치 후 시스템 PATH만으로 버전 1~6 프로젝트, 27장 기존 작품, 세로 3장, 실제 PDF 가져오기와 내장 OCR·번역 확인. 결과는 `qa/portable-result.json`

## 남은 범위

세로 영문·숫자는 글자별로 똑바로 세운다. 주요 괄호·긴 대시·일본어 장음을 돌리고 쉼표·마침표를 오른쪽 위에 놓는다. 일본어 출판용 금칙·루비·세로 안 가로쓰기 및 자동 열바꿈의 커서 위치 세분화는 아직 없다. 글꼴은 Windows에 설치된 글꼴을 사용한다.

이름 있는 배치는 텍스트 상자의 위치·크기·회전·배율을 재사용한다. 다른 크기 카드 자동 비례 맞춤, 그룹을 포함한 템플릿 생성과 상자 내용의 의미에 따른 자동 대응은 후속 범위다. 현재 대응 목록을 확인한 후 적용하는 방식이다. 사용자 반복 조작 시간이 얼마나 줄었는지는 아직 측정하지 않았다.

다음 개발은 설치 마법사와 업데이트 시 개인 작업 보존이다. 카드 순서 변경·삭제, 기존 앱 작업 이전, 작품 전체 자동 번역과 번역 품질 개선도 남아 있다. PDF는 기존과 같이 이미지 기반이며 전체 V1/PowerPoint 수준 편집 완료를 뜻하지 않는다. 배포 검증은 현재 Windows PC의 재배치 실행이며 다른 PC 검증은 별도로 필요하다.

입력 구현은 [Qt 입력 이벤트 계약](https://doc.qt.io/qt-6/qinputmethodevent.html), 문자 경계는 [QTextBoundaryFinder](https://doc.qt.io/qt-6/qtextboundaryfinder.html)를 참고하고 설치된 Qt 6.10.2에서 검증했다.
