# 여덟 번째 구현 — 직접 문장 선택·브러시 지우기 · 2026-09-14

## 0.8.1 수정 · 2026-09-15

PDF 글자 제거에 주변 색 보간을 적용하던 문제를 수정했다. 일반 글자는 PDFium에서 숨겨 실제 배경을 렌더하고, 글자 클리핑으로 색·이미지를 그리는 문서는 별도 복사본의 PDF 명령을 처리한다. 클리핑 전 그림과 선, 같은 그룹의 배경, 중첩 Form은 보존한다. 결과 배경은 별도 PNG 자산으로 작업에 저장한다. 새 선택과 PDF 글자에 칠한 브러시는 실제 배경을 사용하며 정상 배경 픽셀을 다시 칠하지 않는다.

이전 작업은 파일 메뉴의 `PDF 원래 배경 적용`으로 원본 PDF를 연결한다. 파일명 추측 대신 페이지 픽셀 일치를 확인하고 기존 원문 상자의 제거 배경만 다시 만든다. 번역문·배치·서식을 유지하고 한 번에 실행 취소할 수 있다. 기존 브러시 기록은 그대로 둔다. 처리 중 다른 편집을 하면 낡은 결과를 적용하지 않는다.

프로젝트 형식 8은 `Page.clean_asset`으로 실제 PDF 배경을 참조한다. 원본과 배경을 합해 작품당 512MB, 파일당 250MB까지다. 이전 형식 1~7을 열 수 있고 열기만으로 다시 저장하지 않는다. 새 파일은 0.8.1 이상에서 열어야 한다. pypdf 6.10.0과 라이선스를 배포본에 포함한다. 앱에서 이전 웹 편집기를 호출하지 않는다.

새 PDF 배경·저장·기존 작업 복구 검사를 포함한 170개 검사를 통과했다. 사용자 자료 두 종류의 대표 4페이지(글자 클리핑 2장, 일반 글자 2장)는 이전 PDF 교체의 배경 출력과 픽셀 단위로 일치했다. 결과는 `qa/pdf-background-081/real-result.json`에 기록했다. 실제 Windows 편집 창의 선택·브러시·한글 입력·저장·출력 결과는 `qa/pdf-background-081/result.json`, 배포 검사는 `qa/portable-result.json`에 기록한다.

글자가 이미지나 윤곽선으로 합쳐진 자료, 지원되지 않는 PDF 구조에는 픽셀 복원을 사용한다. OCR의 인식 품질은 별도이며 원문 확인이 필요하다. 다음은 0.8.0 최초 구현 기록이다.

치pdf 0.8.0은 사용자가 일본어 문장을 사각형으로 선택하고 한국어를 입력하는 흐름을 기본으로 한다. PDF와 이미지에서 같은 조작을 사용한다. 전체 페이지 자동 인식은 파일 메뉴에서 요청했을 때 실행한다.

## 사용자 흐름

1. 자료를 열고 문장을 사각형으로 드래그한다. 세로 원문은 선택 도구 옆의 방향을 먼저 바꾼다.
2. 선택한 범위를 한 문장 블록으로 OCR 처리한다. 읽은 일본어를 원문 패널에 표시하고 같은 위치에 빈 한국어 상자를 만든다. 인식에 실패해 글자가 비어 있어도 원문을 직접 수정하거나 한국어를 입력할 수 있다.
3. 선택한 사각형의 원본 내용은 주변 배경으로 복원하고 즉시 편집 화면에 표시한다. 빈 상자에서도 제거를 유지한다. 번역 버튼은 선택한 상자만 처리하고, 기존 한국어가 있으면 후보로 보여 준다.
4. 브러시로 지울 부분을 칠하고 지우기를 누른다. 적용 전 분홍색 표시는 출력에 포함하지 않는다. Esc로 선택을 취소하고 Ctrl+Z로 적용한 지우기를 되돌린다. 글자 입력 중에는 Esc로 입력을 끝낸 뒤 상자 생성 자체를 되돌린다.
5. 원본 비교, 저장·재열기, 썸네일, PNG/PDF 출력에서 같은 배경 복원 결과를 사용한다.

## 구현과 저장

- `studio/region_tools.py`는 선택 도구·OCR·선택 문장 번역·브러시 적용을 연결한다. 선택한 도구의 옵션만 보여 준다.
- `studio/canvas.py`는 확대 배율과 상관없이 페이지 좌표로 범위를 계산한다. 브러시 선택 표시는 문서와 분리된 화면 표시다.
- `studio/recognition.py`는 선택 범위 OCR과 마스크 기반 주변 픽셀 복원을 처리한다. 선택 밖의 픽셀과 원본 투명도를 보존한다. 대기 작업을 취소하거나 페이지를 바꾸면 늦은 결과를 버린다. 복원 중 배경이 변경되면 브러시 결과를 적용하지 않고 재시도를 안내한다.
- 프로젝트 형식 7에서 `Page.background_patches`에 브러시 복원을, `TextBox.erase_when_empty`에 빈 상자의 원문 제거 설정을 저장한다. 원본 PNG 자산은 변경하지 않는다. 구형 1~6 형식은 열 수 있으며, 저장 시 형식 7로 이행하고 직전 저장본을 백업한다. 0.7 이하 앱에서는 형식 7 파일을 열 수 없다.
- 기존 설치 식별자·사용자 데이터 경로·Windows 10 1809 이상 x64 지원 조건을 유지한다. 제품 표시와 아이콘은 치pdf를 사용한다.

## 검증

기존 동작 검사와 새 `test_manual_regions.py`, `test_manual_editor.py`로 범위 좌표, 취소, 늦은 작업 결과, 빈 원문 수정, 선택 번역, 브러시 픽셀 보존, 손상 기록 거부, 되돌리기, 저장·재열기를 확인한다.

`verify_manual.py`의 실제 Windows Qt 창에서 생성한 일본어 PDF를 열어 드래그·OCR·로컬 번역·한글 IME 수정·브러시·원본 비교·저장·재열기·PNG/PDF 출력까지 44개 검사를 통과했다. PDF에 포함한 이미지 픽셀과 PNG 출력이 같고 원본 PDF/PNG·프로젝트 원본 자산의 바이트가 유지됐다. 결과는 `qa/manual-08/result.json`에 기록한다. 이동식 배포본의 검사는 `qa/portable-result.json`에 별도로 기록한다.

## 범위와 한계

배경 복원은 주변 픽셀 보간이며 복잡한 그림·무늬를 원래 모습 그대로 생성하지는 않는다. 한 번에 선택한 60만 픽셀까지 처리하며 넓거나 떨어진 영역은 나누어 지운다. 브러시는 원본 페이지에 적용하며 삽입한 객체는 객체 편집으로 수정한다.

OCR·번역은 원문 대조가 필요하다. 실제 시험에서 「鍵」를 「交」로 오인식해 번역도 틀린 사례가 있었고, 원문 패널은 낮은 확신도 안내를 표시했다. 시험 결과에 실제 인식·번역과 직접 수정한 글을 구분해 남겼다. Windows 10 실기 검증은 아직이며 현재 실행 검증 환경은 Windows 11이다.
