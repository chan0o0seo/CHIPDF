# 아홉 번째 구현 — 도장·화면 위치 유지·글꼴 즐겨찾기 · 2026-09-15

치pdf 0.9.0은 배경을 직접 복제하는 도장 도구를 추가하고, 확대 작업 중 실행 취소나 원본 비교로 화면이 다시 맞춤 크기로 돌아가는 흐름을 수정한다. 앞서 추가한 휠 버튼 드래그 이동과 글꼴 즐겨찾기도 포함한다.

## 도장으로 배경 복제

1. `도장`을 선택하고 Alt를 누른 채 복제할 배경 지점을 클릭한다.
2. 칠할 위치에서 드래그한다. 도구 옆에서 도장 크기를 조절한다.
3. 마우스 버튼을 놓으면 한 획을 적용한다. Ctrl+Z로 획 단위로 실행 취소한다.

도장은 현재 편집된 배경 픽셀을 복제한다. 사용자가 삽입한 텍스트·도형·이미지는 배경과 별도이므로 복제 대상에 포함하지 않는다. 도장 결과는 프로젝트와 완성본에 포함되며 원본 비교에서는 편집 전 자료를 표시한다.

같은 카드에서는 도구 변경·원본 비교·실행 취소 후에도 복제 지점을 유지한다. 다른 카드로 이동하면 새로 지정한다. 도장 결과는 기존 `BackgroundPatch`를 타일로 나누어 저장하고 한 획을 한 번의 실행 취소로 묶는다. 프로젝트 형식은 기존 버전 8을 유지한다.

이번 범위는 도장 도구까지다. 클리핑 마스크와 그 밖의 Photoshop 편집 기능은 포함하지 않는다.

## 확대 작업 유지

실행 취소·다시 실행, 원본 비교 전환, 원문 확인 패널 전환으로 편집 화면이 갱신되어도 확대율과 보고 있던 위치를 유지한다. 휠 버튼을 누른 채 드래그하면 상하좌우로 이동하고, 버튼을 놓으면 기존 도구 작업을 이어 간다.

## 글꼴 즐겨찾기

글꼴 옆 ☆로 현재 글꼴을 등록하고 ★로 해제한다. 즐겨찾기 목록이나 `서식 → 글꼴 즐겨찾기`에서 바로 적용하며, 입력 중 선택한 일부 글자에도 적용한다. 목록에서 더 이상 설치되지 않은 글꼴을 삭제할 수 있다. 설정은 `%LOCALAPPDATA%/TranslationStudio/font-favorites.json`에 보관하여 다른 작품과 다음 실행에서도 유지한다.

## 버전과 배포

앱 버전을 0.9.0으로 올리고 현재 사용 안내를 갱신한다. 과거 구현 보고서는 당시 기록으로 보존한다. 이동식 배포본에는 이 보고서도 포함한다.

## 테스트·검증

이번 변경에서는 사용자 요청에 따라 테스트를 작성하거나 실행하지 않으며 수동 실행 검증도 진행하지 않는다. 이전 버전 보고서의 검증 결과는 이번 변경에 대한 결과가 아니다.
