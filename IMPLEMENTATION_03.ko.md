# 세 번째 구현 — 도형·이미지·배치 · 2026-09-12

카드 한 장의 번역 흐름에 **도형·이미지 삽입과 공통 배치 도구**를 추가했다. 기획서 단계 D의 첫 확장이다. 도구를 `삽입`, `배치`, 선택 대상별 서식에 모았다. 전체 PowerPoint 수준 편집이나 정식 V1 완료를 뜻하지 않는다.

## 이번 구현

| 분류 | 가능한 작업 |
|---|---|
| 도형 | 사각형, 둥근 사각형, 타원·원, 선, 화살표. 채우기·테두리·선 두께·불투명도 |
| 이미지 | 삽입·교체, 비율 고정, 이동·크기·회전, 좌우·상하 뒤집기, 드래그로 사각 자르기·전체 복원, 불투명도 |
| 배치 | 다중 선택, 6방향 정렬, 가로·세로 간격, 맨 앞·뒤 및 한 단계 순서 변경, 정밀 좌표·크기·회전 |
| 보호 | 잠금·잠금 해제·전체 해제. 잠긴 대상의 편집과 자동 번역 적용 차단 |
| 편집 편의 | 혼합 대상 복사·잘라내기·붙여넣기, 전체 선택, 취소선, 대표 글자 서식 복사·적용 |
| 저장 | 버전 3 작업, 삽입 이미지 자체 보관, 실행 취소·다시 실행·자동 저장·PNG 출력과 통합 |

도형과 이미지는 텍스트와 공통 변형 코드를 사용한다. 화면과 PNG는 동일한 Qt 장면을 그린다. 원본 인식·제거 좌표는 새 객체의 이동과 독립적이다. 다른 카드에 붙여넣으면 이전 카드의 원문 제거 기록을 제거해 엉뚱한 배경을 지우지 않는다.

0.1·0.2 프로젝트는 메모리에서 읽되 열기만 해서는 파일을 다시 쓰지 않는다. 이전에는 열자마자 새 형식으로 자동 저장되던 동작을 이번 실제 카드 검증에서 발견해 수정했다. 편집·저장 시 버전 3으로 저장하며 직전 파일을 백업한다.

## 검증

**42개 행동 테스트 통과:** 기존 편집·번역 24개와 새 편집 18개. 실제 Qt 마우스로 도형 이동·크기·회전, 이미지 비율 고정과 자르기 드래그를 검증했다. Ctrl+C/V/X, 잠금 보호, 회전한 대상의 정렬, 동일 간격, 순서 변경에 따른 실제 픽셀 색 변화, 후보 번역과 OCR의 공존, 잘못된 이미지 기록 거절, 이전 버전 파일 열기 보존을 확인했다.

사용자 카드의 번역 초안 프로젝트 사본에 텍스트·도형·투명 이미지를 포함한 11개 대상을 배치했다. 자르기·잠금·정렬 후 저장·다시 열기 PNG가 바이트 단위로 같았으며 입력 프로젝트 해시는 그대로였다. 이 예시는 편집 기능 검증용이며 본문의 자동 번역 품질을 승인한 완성본은 아니다.

- `qa/tests-03.log`: 전체 테스트 결과
- `qa/editing-03/result.json`: 실제 카드 사본 검증
- `qa/editing-03/window.png`, `crop-dialog.png`: 네이티브 화면 점검
- `qa/editing-03/편집예시.twproj`, `편집예시.png`: 혼합 객체 저장·출력 표본
- `verify_portable.py`: 재배치한 ZIP, 시스템 기본 PATH, 기존 작업·새 혼합 작업 출력 일치와 OCR·번역 검사. 빌드별 결과는 `qa/portable-result.json`

## 남은 범위

그룹·그룹 해제, 안내선·맞춤, 줄·문단 간격과 내부 여백, 세로쓰기 편집은 단계 D의 남은 작업이다. 선·화살표는 회전 가능한 직선형이며 자유 곡선·연결선은 없다. 서식 복사는 대표 글꼴·크기·색·강조를 복사하며 복잡한 문단 서식 전체를 복제하지 않는다.

PDF·여러 카드·일괄 출력과 설치 마법사는 다음 큰 단계다. 현재 배포는 Python을 따로 설치할 필요 없는 Windows 폴더형 프로그램이다. 실제 다른 PC, 다른 Windows 버전과 서명된 설치 프로그램은 아직 검증하지 않았다.

번역 엔진은 바꾸지 않았다. OCR과 로컬 초안에는 오역이 남아 있으며 원문 대조가 필요하다. 복잡한 배경의 글자 제거 품질도 후속 과제다.
