# 독립 번역 엔진 검토 — 2026-09-12 갱신

## 0.2 추가 검토: 직접 번역 모델을 시험용 초안으로 연결

`facebook/m2m100_418M`을 CTranslate2 int8로 변환해 일본어→한국어를 직접 번역했다. 모델의 공식 카드에는 MIT 조건과 직접 다국어 번역 방식이 명시되어 있다. 고정 revision은 `55c2e61bbf05dfb8d7abccdc3fae6fc8512fd636`이다. 배포 모델·토크나이저·설정은 약 496MB이며 개발용 PyTorch/Transformers와 원본 가중치는 배포하지 않는다. [공식 모델 카드](https://huggingface.co/facebook/m2m100_418M), [CTranslate2 공식 변환 가이드](https://opennmt.net/CTranslate2/guides/transformers.html)

같은 합성 문장 20개를 직접 실행했다. 이번 PC의 모델 로드는 약 0.49초, 문장별 실행은 0.318~0.776초였다. 일부 짧은 질문·주체 표현은 이전 경유 모델보다 개선됐지만, 품질 평가 통과로 판단하지 않는다. 예를 들어 `机の引き出しには…`는 ‘테이블 밖에는…’, `彼女が嘘をついているとは限らない。`는 ‘그녀가 거짓말을 하는 것은 아니다.’로 나왔다. 위치와 가능성의 의미를 바꾸는 오역이다.

**0.2에서는 원문 대조가 필요한 시험용 초안으로만 제공한다.** 재번역은 별도 후보로 표시하고 수동 수정을 덮어쓰지 않는다. 실제 카드에서도 ‘元上司’를 ‘전직장’으로 번역하는 오류가 남았다. 제목의 OCR 오류도 있어, 낮은 인식 신뢰도는 확인을 요청한다. 높은 신뢰도도 정확성을 보장하지 않는다.

합성 시험 결과는 `qa/m2m-benchmark.json`, 실제 카드 작업 기록은 `qa/workflow-02/result.json`이다. 합성 시험은 항목별 단일 입력이고, 앱에서는 일본어 문장부호·줄바꿈으로 나눠 번역한다. 긴 입력은 조용히 잘라내지 않고 원문 분할을 안내한다. 서로 다른 입력 분할 방식의 속도·품질을 동일 조건의 점수처럼 비교하지 않는다.

이 앱은 사용자 문서를 외부로 전송하지 않는다. 더 나은 번역 품질, 용어집, 장문·이름·부정 표현의 보존은 후속 검증 과제다. 아래는 0.1 때 시험한 경유 모델에 대한 기록이며, 이 모델은 계속 배포에서 제외한다.

## 0.1 기록: 경유 번역 모델

**로컬 CPU에서 번역 실행은 확인했지만, 시험한 경유 번역 모델은 기본 엔진으로 채택하지 않는다.** 머미 자료의 단서·부정 표현·행동을 바꾸거나 생략하는 사례가 나왔다. 새 편집기 배포본에는 이 모델을 포함하지 않았다.

## 실제 실행한 후보

Argos 공개 모델 1.1의 일본어→영어와 영어→한국어를 CTranslate2 4.6.2, CPU int8, 4개 스레드, beam 4로 연결했다. SentencePiece 0.2.1을 사용했다. Argos는 언어 간 직접 모델이 없을 때 경유 번역을 지원하며 이때 품질 손실 가능성을 설명한다. [공식 저장소](https://github.com/argosopentech/argos-translate), [공식 모델 목록](https://raw.githubusercontent.com/argosopentech/argospm-index/main/index.json)

| 측정 | 이번 PC의 결과 |
|---|---|
| 표본 | 머미 형식의 직접 작성한 일본어 문장·짧은 문단 20개 |
| 모델 로드 | 0.561초 |
| 20개 순차 번역 합계 | 2.180초 |
| 문장별 평균 | 0.109초 |
| 문장별 범위 | 0.066~0.172초 |
| 다운로드 | 두 모델 합계 237,944,725바이트, 약 227MiB |
| 문서 외부 전송 | 없음. 공개 모델만 다운로드하고 번역은 로컬 실행 |
| 메모리·클린 PC | 이번 시험에서 측정/검증하지 않음 |

속도는 작은 합성 표본을 한 번 실행한 수치로, 실제 작품 전체의 처리 시간이나 품질 점수가 아니다. 사용자 자료 본문을 시험용 번역 서비스로 전송하지 않았다.

## 발견한 품질 문제

| 원문 의도 | 실제 출력 일부 | 판단 |
|---|---|---|
| 범인은 아직 이 저택 안에 있다 | `??? 필은 여전히이 집에 있습니다.` | 핵심 주체 훼손 |
| 오후 8~9시에 어디 있었는가 | `오후 8시부터 오후 9시까지` | 질문 전체 누락 |
| 그녀가 거짓말한다고 단정할 수 없다 | `그녀는 lying하지 않습니다.` | 부정/가능성 의미 변경, 영어 혼입 |
| 레지나는 슈에게 편지를 건넸다 | `Regina는 Shu에게 편지를 썼습니다.` | 건넴을 작성으로 변경 |
| 아무도 죽이지 않았다. 믿어 달라 | `나는 누구를 죽이지 않는다.` | 시제 변경과 두 번째 문장 누락 |

전체 20개 원문·출력·시간은 개발 폴더의 `qa/engine-benchmark.json`에 남겼다. 이 판단은 원문 대조에 의한 개발 검토이며, 독립 번역가의 품질 평가나 정식 벤치마크 점수가 아니다.

## 기술적으로 확인한 점

첫 공식 목록의 배포 주소는 HTTP 403을 반환해 같은 운영자의 [공개 모델 배포 서버](https://data.argosopentech.com/argospm/v1/)를 사용했다. 파일 크기·SHA-256·주소는 `vendor/models/downloads.json`에 기록했다.

SentencePiece는 이 Windows 환경에서 일본어가 포함된 경로를 직접 열지 못했다. Python에서 모델 바이트를 읽고, SentencePiece의 serialized model과 CTranslate2의 `files` 인수로 넘겨 해결했다. 따라서 한글·일본어 프로젝트 경로를 영문으로 바꾸도록 사용자에게 요구할 필요가 없다. [CTranslate2 Translator API](https://opennmt.net/CTranslate2/python/ctranslate2.Translator.html)

## 다음 후보와 결정 기준

| 방식 | 이번 확인 수준 | 다음 판단 |
|---|---|---|
| Argos 일본어→영어→한국어 | 실제 실행·20개 대조 | 기본 엔진 채택 보류 |
| 직접 다국어 번역 모델 | 문서 검토만 | 경유 번역 없이 단서·고유명사·부정 표현을 보존하는 후보 시험 |
| 사용자가 선택하는 외부 엔진 | 미연결 | 제공자·비용·전송 범위를 사용자가 선택한 경우에 연결 |

검토한 NLLB-200 distilled 600M의 공식 모델 카드는 연구 목적이며 프로덕션 배포용으로 공개한 모델이 아니라고 설명하고, CC-BY-NC 조건과 긴 문서 번역의 한계도 명시한다. 따라서 모델 이름만 보고 기본 배포 모델로 고정하지 않았다. 이번에 다운로드하거나 품질 시험하지도 않았다. [공식 모델 카드](https://huggingface.co/facebook/nllb-200-distilled-600M)

기본 엔진은 아직 미선정이다. 다음 후보에서도 같은 20개 문장에 더해 장문·용어집·사용자 수정 보존과 실제 설치 자원을 검증한 뒤 결정한다. UI에 자동 번역을 연결하는 단계 C의 선행 과제다.

## 시험 재현

개발 폴더에서 실행한다. 모델 다운로드는 약 227MiB이며 추론 라이브러리는 별도로 설치한다.

```powershell
python -m pip install --target .engine-deps -r requirements-engine.txt
python engine_probe.py --download --benchmark
```

다시 측정할 때는 `python engine_probe.py --benchmark`로 이미 받은 모델을 사용한다. 모델과 엔진 시험은 편집기의 실행·빌드와 독립되어 있다.
