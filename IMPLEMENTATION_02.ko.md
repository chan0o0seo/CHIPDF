# 두 번째 구현 — 카드 한 장 번역 흐름 · 2026-09-12

별도 폴더의 Windows 앱에서 **카드 열기 → 일본어 인식 → 번역 초안 → 직접 수정 → PNG 저장**을 연결했다. 상단에는 주 작업 버튼을 유지하고, 원문·번역 후보는 필요할 때 오른쪽 패널에서 확인한다. 기획서의 단계 C 중 이미지 카드 한 장 경로를 구현한 시제품이며 전체 제품 완료는 아니다.

## 구현

- 이미지에서 일본어 문단을 자동 인식하고 원문·위치·신뢰도를 보존한다. 불확실한 인식은 확인 대상으로 표시한다. 신뢰도가 높더라도 정확성을 보장하지 않는다.
- 일본어→한국어 로컬 번역 초안을 빈 상자에만 넣는다. 직접 입력·서식 수정·원문 수정 중 도착한 오래된 결과는 적용하지 않는다.
- 다시 번역한 결과는 후보로 보관하고, 사용자가 적용할 때만 기존 문장을 바꾼다. 원문을 수정하면 오래된 후보를 지운다.
- 원문 획을 주변 색으로 복원한다. OCR이 놓친 글자도 같은 줄 안에서 제거하며 긴 밑줄은 보존한다. 상자 이동과 원문 제거 위치는 독립적이다. 제거 영역 밖 픽셀과 알파 값은 보존한다.
- OCR·번역은 백그라운드에서 진행하며 취소할 수 있다. 번역 중단 시 완료된 문단은 보존한다. 카드 전환·실행 취소 뒤의 이전 결과는 버린다.
- `.twproj` 버전 2에 원문, 초안, 후보, 검토 상태와 제거 기록을 저장한다. 버전 1 작업도 열 수 있다. 손상된 제거 기록은 현재 작업을 바꾸기 전에 거절한다.

## 검증

기본 편집 12개와 새 흐름 12개, 총 **24개 행동 테스트 통과**. 한글 IME, 부분 서식, 상자 조작, 저장 실패 보존, 실행 취소, 중단 후 이어 번역, 처리 중 수동 입력, 원문 변경, 카드 전환, 후보 적용, 이전 프로젝트 호환, 제거 영역·알파 보존을 확인했다.

사용자 자료에서 가져온 실제 카드 사본으로 원문 4영역을 인식했다. 이번 PC에서 OCR 약 0.58초, 첫 모델 준비를 포함한 초안 생성 약 3.56초였다. 카드 한 장을 한 번 실행한 결과이며 전체 작품 처리 속도를 뜻하지 않는다. 제목 OCR 오류는 검증 과정에서 **명시적으로 원문을 교정한 뒤** 번역했다. 번역문을 직접 고친 후 재번역해도 문장이 유지됐고, 저장·다시 열기 PNG는 바이트 단위로 같았다. 입력 파일 해시도 유지됐다.

근거: `qa/workflow-02/result.json`, `window.png`, `직접수정.twproj`, `직접수정.png`. `qa_workflow.py`로 실제 UI와 같은 작업 큐를 검증한다. 개발 테스트는 `python -m unittest test_studio.StudioTests test_workflow.WorkflowTests -v`로 실행한다.

배포 검증은 `verify_portable.py`에서 ZIP을 새 경로에 풀고 개발용 Python/Qt 환경 변수를 제거한 상태로 실행한다. 이전 프로젝트·새 프로젝트의 PNG 일치와 배포본 자체 OCR·번역 실행을 검사하며, 빌드별 결과를 `qa/portable-result.json`에 남긴다. 별도 클린 PC·오래된 Windows·다른 하드웨어에서의 검증은 아직 하지 않았다.

## 남은 범위

로컬 번역은 시험용 초안이다. 실제 카드에서도 위치·시제·비유 등의 오역이 남는다. 단서·고유명사·부정 표현의 품질 기준을 통과한 번역 엔진으로 보아서는 안 된다. 원문 대조가 필요하며 외부 유료 번역은 연결하지 않았다.

원문 제거는 밝고 비교적 단순한 배경을 중심으로 시험했다. 사진·복잡한 삽화·테두리와 글자가 겹친 경우에는 흔적이나 손상이 생길 수 있다. 수동 제거 범위 편집과 정밀 배경 복원은 남아 있다. 세로 일본어 읽기는 시험 기능이고 세로쓰기 편집은 아직 아니다.

PDF·여러 카드·일괄 출력·도형·이미지 객체·정렬/분배·그룹·세로 편집·설치 마법사는 후속 단계다. 현재 배포는 Python 없이 실행하는 Windows 폴더형 앱이다.
