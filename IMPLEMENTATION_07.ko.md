# 일곱 번째 구현 — Windows 설치·업데이트·작업 보존 · 2026-09-14

0.7은 편집기를 한국어 Windows 설치 마법사로 설치하고 시작 메뉴에서 실행하도록 만든다. 프로그램 파일과 작업 데이터를 분리해 업데이트·제거·재설치 시 원본과 작업, 백업, 개인 서식을 유지한다. 편집 화면과 버전 6 프로젝트 형식은 그대로 사용한다.

## 동작과 구현

| 작업 | 구현 |
|---|---|
| 설치 | Windows 11 x64, 현재 사용자 계정에 설치. 관리자 권한 불필요. 기본 위치 `%LOCALAPPDATA%/Programs/Translation Studio` |
| 실행 | 시작 메뉴, 선택한 경우 바탕 화면 바로 가기. Python·개발 도구 별도 설치 불필요 |
| 업데이트 | 새 Setup 실행, 동일 AppId·기존 경로·바로 가기 선택 유지. 더 이전 버전이나 다른 경로로 덮어쓰는 업데이트 거절 |
| 실행 중 보호 | 앱 프로세스가 이름 있는 Windows mutex를 유지. 편집기가 열려 있으면 설치와 제거가 종료 안내. 강제 종료·자동 재시작 없음 |
| 제거 | Windows 설치된 앱에서 제거. 설치 기록에 있는 프로그램 파일·바로 가기만 제거 |
| 작업 보존 | 직접 저장한 `.twproj`, 자동 저장·백업·개인 서식·최근 작업 경로 유지. 설치 폴더에 사용자가 추가한 파일도 보존 |
| 재설치 | 작은 설치 식별 INI를 남겨 사용자 파일이 남은 이전 설치 폴더를 구분. 같은 폴더로 재설치 후 이전 작업과 서식 사용 |
| 경로 보호 | 프로필·문서·설정 폴더 자체, 연결된 경로, 비어 있지 않은 다른 폴더에 설치 거절 |

`installer/translation-studio.iss`는 Inno Setup 7.1.0의 비관리자 설치 모드와 기본 설치 기록을 사용한다. `InstallDelete`와 `UninstallDelete`의 일괄 삭제 규칙을 사용하지 않는다. `translation-studio.install.ini`만 제거 후에도 남기며 파일에는 AppId·버전·빌드·mutex 이름만 저장한다. 작품이나 개인 서식은 설치 파일에 포함하지 않는다.

`studio/runtime.py`는 앱 시작 시 mutex를 만들고 Qt와 작업자 종료를 포함한 프로세스 수명 동안 유지한다. 여러 편집기를 열 수 있고 모두 닫혀야 설치·제거할 수 있다. `main.py`의 QA 실행은 분리된 데이터 위치와 읽은 서식 목록을 보고한다. 실제 제품 AppId는 고정하고, 검증용 AppId·바로 가기·mutex는 컴파일 시 별도 값으로 만든다.

`prepare_installer.py`는 공식 고정 버전 파일의 SHA-256와 Pyrsys B.V. 배포자 서명을 확인하고 개발 폴더에만 이동식 컴파일러를 준비한다. `build_installer.py`는 기존 배포본을 복사한 전용 임시 폴더로 설치 파일을 만든다. 이전 릴리스는 수정하지 않으며 설치 파일·SHA-256 목록·메타데이터·컴파일 로그를 빌드별로 보관한다.

## 검증 자료

- `test_runtime.py`: 실제 Windows mutex의 여러 핸들 수명, 개인 설치/QA 이름 분리, 손상된 설치 식별 파일 처리
- `test_installer.py`: 검증 도구가 실제 제품 AppId나 위조·변경된 설치 파일, 다른 경로·등록 정보를 거절하는지 검사
- `qa/tests-07.log`: 기존 편집 기능을 포함한 전체 행동 테스트 결과
- `verify_installer.py`: 별도 QA 설치 등록으로 0.6→0.7 설치·업데이트·제거·재설치. 작업·백업·개인 서식 해시 보존, 실제 설치 앱 실행, 시작 메뉴 대상 확인, 실행 중·다운그레이드·위치 변경 거절 검사
- `qa/installer-result.json`: 실제 설치 순환 검증 결과와 해당 시험 폴더·로그·네이티브 창 캡처
- `verify_portable.py`, `qa/portable-result.json`: 개발 런타임 경로 없이 배포본 재배치 실행, 기존 버전 1~6 출력·여러 카드·PDF·OCR·번역 회귀 확인

시험용 설치는 제품과 다른 AppId와 바로 가기 이름을 사용한다. 실제 개인 데이터 대신 시험 폴더의 작업·서식 사본을 사용하고 해당 시험용 제거 프로그램만 실행한다. 사용자의 실제 프로그램 설치 여부나 기존 작업을 바꾸지 않는다. 상세 통과 여부·해시·빌드 식별자는 위 JSON과 로그가 기준이다.

2026-09-14 최종 검증에서 전체 113개 테스트와 실제 설치 순환의 13개 검사를 통과했다. `qa/install-7269a94597`에서 0.6 설치, 0.7 업데이트, 제거, 재설치를 수행했고 마지막에는 QA 등록과 바로 가기를 제거했다. 보존 대상 작업·복구 파일·백업·서식 5개는 바이트 단위로 유지됐다. 설치된 이전 버전·업데이트본·재설치본에서 세로쓰기 카드 3장의 출력이 기존 결과와 일치했고, 저장한 작품을 다시 열어 출력했으며 0.7에서는 개인 서식을 다시 불러오는 것을 확인했다.

시험 중 발견한 설치 경로 상수 오류를 수정했다. 검증 도구는 바로 가기를 Unicode `IShellLinkW`로 읽고, 정상 자동 저장이 일어나는 복구 파일과 실행 시험용 일반 저장 문서를 분리한다. 설치 거절은 단순 종료 코드뿐 아니라 의도한 거절 사유를 확인한다. 배포 설치 파일은 `dist/installer-20260914-065235-7bc6e1/Translation-Studio-0.7.0-Setup.exe`이며, `latest-installer.json`과 `qa/release-07.json`에 배포본·검증 결과를 연결한다. 제품 AppId의 실제 설치는 수행하지 않았다.

## 남은 범위

업데이트는 사용자가 새 설치 파일을 실행하는 방식이며 온라인 버전 확인·자동 다운로드는 아직 없다. 설치 파일은 코드 서명되지 않은 시험 배포본이다. 다른 PC·Windows 버전·기업 보안 정책 아래의 검증, `.twproj` 파일 연결, 기존 웹 앱의 작업 가져오기는 후속 범위다. 설치 프로그램은 과거 프로젝트 형식을 자동 변환하거나 번역 품질을 변경하지 않는다.

다음 단계는 카드 순서 변경·삭제와 작품 작업 흐름을 다듬는 것이다. 전체 V1과 PowerPoint 수준 편집 범위의 완료를 뜻하지 않는다.

구현 근거: [비관리자 설치 모드](https://jrsoftware.org/ishelp/topic_admininstallmode.htm), [AppId와 업데이트 기록](https://jrsoftware.org/ishelp/topic_setup_appid.htm), [실행 중 앱 보호](https://jrsoftware.org/ishelp/topic_setup_appmutex.htm), [이동식 설치 제작 도구](https://jrsoftware.org/ishelp/topic_technotes.htm). 설치 도구 고지는 `THIRD_PARTY.md`와 설치본 `licenses/Inno-Setup-LICENSE.txt`에 포함한다.
