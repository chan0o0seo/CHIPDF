# 다섯 번째 구현 — PDF·여러 카드·작품 저장 · 2026-09-13

사용자와 합의한 0.5 범위인 **PDF·이미지 폴더 가져오기, 카드 목록에서 페이지 전환, 작품 단위 저장, PNG·PDF 묶음 출력**을 구현했다. 화면은 왼쪽 카드 목록과 가운데 편집 화면을 유지한다. 파일 메뉴에 폴더 열기·자료 추가를 넣었고 출력할 때만 범위·형식을 묻는다.

## 동작

| 작업 | 구현 내용 |
|---|---|
| 가져오기 | PDF와 여러 이미지 선택, 이미지 폴더의 숫자 순서, 여러 파일·폴더 드롭. 작업자로 처리하며 중단·손상 파일 실패 시 기존 작품 유지 |
| 카드 전환 | 썸네일 클릭, Ctrl+PageUp/PageDown. 한글 입력을 마친 뒤 페이지를 전환하고 페이지별 객체·그룹·부분 서식 유지 |
| 작품 저장 | 버전 5 프로젝트에 페이지별 원본과 편집 정보를 함께 보관. 모든 페이지 자동 저장, 원본 파일이 없어도 재열기, 이전 버전 읽기 |
| 실행 취소 | 작품 전체 기록을 유지하며 되돌릴 편집이 있는 카드로 이동. 자료 추가도 한 번에 되돌리기 |
| 처리 보호 | OCR·번역 작업이 시작된 작품·페이지·세대를 확인. 다른 카드로 이동하면 기존 처리를 중단하고 늦게 도착한 결과 적용 차단 |
| 출력 | 전체·현재·선택 카드, PNG 파일/새 묶음 폴더 또는 PDF. 원래 카드 순서와 PDF 인쇄 크기 유지 |
| 출력 확정 | 임시 위치에서 전체 결과를 만든 뒤 교체. 취소·실패 시 기존 PDF와 기존 출력 폴더 보존 |

PDFium 5.13.0 래퍼로 PDF를 기본 200dpi로 렌더링하고, 매우 큰 페이지는 한 변 6,000px·2,400만 픽셀로 제한한다. PDF의 페이지 방향과 포인트 단위 크기를 저장한다. 파일 가져오기는 백그라운드에서 수행하며 PDFium 핸들은 하나의 잠금 안에서 생성·사용·해제한다. 화면 객체가 아닌 문서 모델과 원본 데이터만 주고받는다.

출력은 편집기와 같은 Qt 항목 렌더러를 사용하되 현재 화면을 다른 카드로 돌려가며 출력하지 않는다. 시작 시 편집 상태를 복사한 후 각 페이지를 렌더링한다. ReportLab으로 페이지별 이미지를 PDF에 넣으며 원본 PDF의 물리적 크기를 유지한다. PDF의 원래 벡터·글꼴·링크를 편집하거나 복원하는 기능은 아니다.

## 검증

**72개 행동 테스트 통과:** 기존 57개와 여러 카드/PDF 관련 15개. 페이지 클릭 중 한글 입력 확정, 다른 페이지에서 실행 취소, 그룹 복사·문단 변경, 여러 페이지 자동 저장, 원본 파일 제거 후 재열기, 지연 OCR·번역의 페이지 혼동 방지, 가져오기 취소, 추가 실행 취소, 자연 정렬, 손상 프로젝트 거절, PNG 부분 출력의 픽셀 일치, PDF 페이지 크기, 출력 중단 시 기존 결과 보존을 확인했다.

실제 진행 자료 PDF 11페이지와 PL 자료 PDF 6페이지, 카드 이미지 10장을 합친 27페이지를 검증했다. 원본을 읽고 별도 QA 프로젝트에 한글 검증 표식을 추가했다. 전체 저장·재열기 PNG와 묶음 출력 PNG의 해시가 모두 일치했다. 모든 입력 파일 해시는 그대로였다. 출력 PDF는 27페이지이며 모든 페이지의 인쇄 크기를 별도 PDF 파서로 확인했다. 대표 세 페이지를 Poppler로 렌더링해 네이티브 미리보기와 함께 검토했다. 본문은 원문을 보존한 기능 시험 자료이며 번역 완료본이 아니다.

- `qa/tests-05.log`: 전체 테스트 결과
- `qa/collection-05/result.json`: 27페이지 검증 결과·해시·물리적 크기
- `qa/collection-05/pdf-window.png`, `cards-window.png`, `export-dialog.png`: 네이티브 화면
- `qa/collection-05/작품.twproj`: 별도 검증 프로젝트
- `verify_portable.py`: 새 ZIP을 다른 폴더에서 시스템 PATH만으로 실행. 버전 1–4 결과 유지, 버전 5 전체 27장 PNG/PDF 출력, 실제 6페이지 PDF 가져오기, 내장 OCR·번역 검사. 빌드별 결과는 `qa/portable-result.json`

## 범위와 다음 작업

한 작품은 200장·원본 합계 512MB까지 지원한다. 폴더 가져오기는 바로 안의 이미지만 포함한다. PDF는 이미지 기반 가져오기/출력이며 새 편집을 이어갈 파일은 `.twproj`다. 세로쓰기 편집, 서식·배치 재사용, 카드 순서 변경·삭제, 작품 전체 자동 번역, 기존 앱 작업 이전, 설치 마법사는 남아 있다. 이번 구현은 단계 E의 확장이며 정식 V1 완료를 뜻하지 않는다.

로컬 OCR·번역의 품질 제한은 이전과 같다. 여러 페이지의 소스와 편집 정보를 프로젝트에 묶었지만 작품의 번역 품질 전체를 검토한 것은 아니다. 배포 검증은 현재 Windows PC에서 개발 런타임을 참조하지 않는 재배치 실행으로 수행하며, 다른 PC·Windows 버전과 서명된 설치 패키지는 후속 검증이다.

구현 참고: PDFium의 직렬 호출과 페이지 렌더링은 [공식 Python API](https://pypdfium2.readthedocs.io/en/stable/python_api.html)의 계약을 따르고 로컬 5.13.0으로 검증했다. PDF 런타임 고지는 `THIRD_PARTY.md` 및 배포본 `licenses`에 포함한다.
