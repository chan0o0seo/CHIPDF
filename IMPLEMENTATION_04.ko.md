# 네 번째 구현 — 그룹·안내선·문단 · 2026-09-12

기획서 단계 D의 다음 범위로 **그룹과 안쪽 편집, 맞춤 안내선, 줄·문단 간격과 여백**을 구현했다. 배치 도구와 서식 메뉴에 모았으며 그룹 안을 편집할 때만 끝내기 버튼을 표시한다. 기본 작업 흐름은 카드 열기 → 번역 초안 → 화면에서 수정 → 저장이다.

## 이번 구현

| 분류 | 동작 |
|---|---|
| 그룹 | 텍스트·도형·이미지 묶기와 해제, 그룹 단위 이동·비율 유지 크기 조절·회전·불투명도·복사·삭제 |
| 안쪽 편집 | 그룹 더블클릭 후 구성 요소 편집, Esc 또는 끝내기 버튼으로 복귀, 8단계 중첩 그룹 |
| 배치 보존 | 그룹 해제 시 이동·회전·배율·불투명도를 구성 요소에 반영. 안쪽 요소 이동 시 다른 요소의 화면 위치 보존 |
| 안내선 | 현재 편집 범위의 대상·페이지 경계 및 중심 맞춤, 화면 기준 6px 감지, 다중 선택의 상대 위치 유지, Alt로 일시 해제 |
| 문단 | 줄 간격 0.5–4배, 문단 앞·뒤 간격, 상자 안쪽 여백. 선택한 텍스트 상자의 모든 문단에 적용하며 부분 글자 서식 유지 |
| 데이터 | 버전 4 저장, 버전 1–3 읽기. 열기만 하면 원래 프로젝트 파일을 다시 쓰지 않음 |

평면 객체 목록에서 그룹의 식별자를 참조한다. 그림과 글자는 부모 그룹의 좌표계에서 편집하고, 원문 인식·제거 영역은 원본 카드의 좌표를 유지한다. 그룹의 배율을 해제 후에도 따로 보존해 글꼴 크기나 줄바꿈을 근사값으로 다시 계산하지 않는다. 원문과 번역을 고친 이후 도착한 자동 처리 결과는 기존 보호 규칙을 따르며, 잠긴 그룹 안의 대상에도 적용하지 않는다.

안내선과 그룹 편집 테두리는 화면에서만 표시한다. 글자를 드래그해 선택할 때는 상자 맞춤을 적용하지 않는다. 다른 카드로 그룹을 붙일 때에는 모든 하위 텍스트의 이전 카드 원문·제거 연결을 제거한다. 붙여넣기 결과가 중첩 제한을 넘으면 현재 작업을 바꾸지 않고 거절한다.

## 검증

**57개 행동 테스트 통과:** 기존 42개와 새 레이아웃 15개. 실제 Qt 마우스로 그룹 선택·이동·크기·회전, 더블클릭 진입, 한글 입력과 Esc 복귀를 검증했다. 중첩 그룹 해제, 회전된 그룹 안쪽 이동, 이미지 자르기·뒤집기, 실행 취소, 복사·붙여넣기·삭제, 문단 서식과 후보 번역의 공존, 안내선의 확대 배율·Alt 동작과 출력 제외를 확인했다.

실제 카드의 v0.3 편집 프로젝트를 읽고 즉시 별도 QA 파일로 저장한 뒤 작업했다. 14개 대상에 3개 그룹을 만들고 중첩·배율 0.94·회전 -2도·줄 간격 1.15배를 적용했다. 저장 후 재열기 PNG가 바이트 단위로 같았고, 입력 프로젝트 해시는 변하지 않았다. Windows 네이티브 창에서 편집 화면·문단 대화상자를 확인하고 복사 데이터가 남아 있는 상태에서도 정상 종료했다. 본문은 이전 시험용 번역을 보존한 기능 검증 표본이다.

헤드리스 테스트 플랫폼의 클립보드 종료 오류는 테스트 데이터 정리로 분리했다. 실제 Windows 창에서도 별도 재현 검사를 했으며 종료 코드 0을 확인했다.

- `qa/tests-04.log`: 전체 57개 테스트 결과
- `qa/layout-04/result.json`: 실제 카드 사본 검증
- `qa/layout-04/window.png`, `inside-group.png`, `guides.png`, `paragraph-dialog.png`: 네이티브 화면
- `qa/layout-04/그룹편집.twproj`, `그룹편집.png`: 중첩 그룹·문단 설정 저장 및 출력 표본
- `verify_portable.py`: ZIP을 다른 폴더에 풀고 시스템 PATH만으로 실행. 버전 1·2·3·4 출력 일치, 입력 파일 보존, 내장 OCR·번역 검증. 빌드별 결과는 `qa/portable-result.json`

## 남은 범위

세로쓰기 편집, PDF·여러 카드·서식 재사용·일괄 출력, 기존 앱 작업 가져오기, 설치 마법사는 후속 작업이다. 그룹 크기 조절은 비율을 유지한다. 문단 도구는 선택한 상자 전체에 적용하며 개별 문단 선택 전용 패널은 없다. 자유 곡선·연결선도 아직 지원하지 않는다. 단계 D 전체 또는 정식 V1 완료를 뜻하지 않는다.

배포는 자체 런타임을 포함한 Windows 폴더형 프로그램이다. 실제 별도 PC나 다른 Windows 버전, 서명된 설치 패키지 검증은 남아 있다. OCR·로컬 번역 초안의 품질과 복잡한 배경 복원은 이전 제한을 유지한다.

## 구현 참고

Qt 6.10.2에 고정해 로컬 API와 실제 동작을 확인했다. 부모 항목 좌표계는 [QGraphicsItem](https://doc.qt.io/qt-6/qgraphicsitem.html#setParentItem), 문단 배율과 여백은 [QTextBlockFormat](https://doc.qt.io/qt-6/qtextblockformat.html#setLineHeight)의 동작을 따른다.
